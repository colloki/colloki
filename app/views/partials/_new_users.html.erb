<% for user in users %>
  <%= link_to gravatar_image_tag(user.email, :gravatar => { :size => 35 }),
  user,
  :title => user.login,
  :class => 'avatar has-tooltip',
  'data-placement' => 'bottom' %>
<% end %>
