<% for user in users %>
  <%= link_to image_tag(user.get_image_url), "/users/#{user.login}",
    :title => user.login,
    :class => 'avatar has-tooltip',
    'data-placement' => 'bottom'
  %>
<% end %>
