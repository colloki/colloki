
Let us know! If you've an <a href="http://en.wikipedia.org/wiki/RSS">RSS</a> feed, we'll add your blog to our list of news and discussion sources.
<br><br>

<a href="#local-publisher-modal" class='btn btn-primary' data-toggle="modal">
  Suggest a Blog
</a>

<div class="modal hide" id="local-publisher-modal">
  <div class="modal-header">
    <a data-dismiss="modal" class="close">&times;</a>
    <h3>Suggest a Blog</h3>
  </div>
  <%= form_tag :controller => :notifier, :action => :send_blog_suggestion, :class=>'form-horizontal' do %>
    <div class="modal-body">
      <p>
        <div class="control-group">
          <label>
            Link <span class="important">*</span>
          </label>
          <div class="controls">
            <%= text_field :blog, :url, :class => "span10" %>
          </div>
        </div>

        <div class="control-group">
          <label>
            Your email <span class="important">*</span>
            <span class="help-inline">So that we can get back to you!</span>
          </label>
          <div class="controls">
            <%= text_field :blog, :email, :class => "span10" %>
          </div>
        </div>

        <div class="control-group">
          <label>
            Description
          </label>
          <div class="controls">
            <%= text_area :blog, :description, :class => "span10", :rows => 7 %>
          </div>
        </div>

      </p>
    </div>
    <div class="modal-footer">
      <button type="submit" class="btn btn-primary">Send</button>
      <a href="#" data-dismiss="modal" class="btn">Close</a>
    </div>
  <% end %>
</div>

<script type="text/javascript">
  $("#local-publisher-modal").on('shown', function () {
    $(this).find('#blog_url').focus();
  });
</script>
