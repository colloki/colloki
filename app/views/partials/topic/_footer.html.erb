
<script>
  $(document).ready(function(e) {
    <%= render :partial => "partials/topic/js", :locals => {
      :current_user => current_user,
      :topic => (defined? topic) ? topic : nil
    } %>
  });
</script>
