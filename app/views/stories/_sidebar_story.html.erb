<div class="sidebar-story row-fluid">
  <div class="span1">
    <% if story.kind == Story::Post %>
      <%= link_to image_tag(story.user.get_image_url), "/users/"+story.user.login, :title => story.user.login, :class => "favicon" %>
    <%else%>
      <%= link_to image_tag(favicon_url(story.source_url), :class => "favicon"), story.source_url %>
    <%end%>
  </div>

  <div class='span9 sidebar-story-content'>
    <%= link_to story.title, story %>
  </div>

  <div class="span2" style="margin-left: 0">
    <% if story.image_exists? %>
      <%= link_to image_tag(story.image.url(:thumb), :class=>'sidebar-story-thumb'), story %>
    <%end%>
  </div>
</div>
