<%= render :partial => "partials/topic/body", :locals => {
  :stories_with_photos => @stories_with_photos,
  :stories => @stories,
  :activity_items => @activity_items,
  :tags => @tags,
  :new_users => @new_users
} %>

<%= render :partial => "partials/topic/footer",
  :locals => {
    :stories => @stories,
    :stories_with_photos => @stories_with_photos
  } 
%>
